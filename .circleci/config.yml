# Warning: automatically generated file
# Please edit .circleci/config.yml.j2, then run bin/generate_circleci_config.py
---
version: 2.1

orbs:
  python: circleci/python@2.0.3
  path-filtering: circleci/path-filtering@0.1.3

executors:
  docker-executor:
    docker:
      - image: cimg/python:3.9

parameters:
  overwrite_tags:
    type: boolean
    default: false
    description: "Set True if you want to overwrite existing docker image tag."

workflows:
  version: 2.1
  generate-config:
    jobs:
      - run_pre_commit
      - path-filtering/filter:
          base-revision: main
          config-path: .circleci/continue-config.yml
          mapping: |
            alertmanager/.* image alertmanager overwrite_tags << pipeline.parameters.overwrite_tags >>
            auth-sidecar/.* image auth-sidecar overwrite_tags << pipeline.parameters.overwrite_tags >>
            awsesproxy/.* image awsesproxy overwrite_tags << pipeline.parameters.overwrite_tags >>
            blackbox-exporter/.* image blackbox-exporter overwrite_tags << pipeline.parameters.overwrite_tags >>
            configmap-reloader/.* image configmap-reloader overwrite_tags << pipeline.parameters.overwrite_tags >>
            curator/.* image curator overwrite_tags << pipeline.parameters.overwrite_tags >>
            dind-golang/.* image dind-golang overwrite_tags << pipeline.parameters.overwrite_tags >>
            dogstatsd/.* image dogstatsd overwrite_tags << pipeline.parameters.overwrite_tags >>
            elasticsearch/.* image elasticsearch overwrite_tags << pipeline.parameters.overwrite_tags >>
            elasticsearch-exporter/.* image elasticsearch-exporter overwrite_tags << pipeline.parameters.overwrite_tags >>
            fluentd/.* image fluentd overwrite_tags << pipeline.parameters.overwrite_tags >>
            git-daemon/.* image git-daemon overwrite_tags << pipeline.parameters.overwrite_tags >>
            git-sync/.* image git-sync overwrite_tags << pipeline.parameters.overwrite_tags >>
            grafana/.* image grafana overwrite_tags << pipeline.parameters.overwrite_tags >>
            init/.* image init overwrite_tags << pipeline.parameters.overwrite_tags >>
            keda/.* image keda overwrite_tags << pipeline.parameters.overwrite_tags >>
            keda-metrics-apiserver/.* image keda-metrics-apiserver overwrite_tags << pipeline.parameters.overwrite_tags >>
            kibana/.* image kibana overwrite_tags << pipeline.parameters.overwrite_tags >>
            kube-state/.* image kube-state overwrite_tags << pipeline.parameters.overwrite_tags >>
            kubed/.* image kubed overwrite_tags << pipeline.parameters.overwrite_tags >>
            nats-exporter/.* image nats-exporter overwrite_tags << pipeline.parameters.overwrite_tags >>
            nats-server/.* image nats-server overwrite_tags << pipeline.parameters.overwrite_tags >>
            nats-streaming/.* image nats-streaming overwrite_tags << pipeline.parameters.overwrite_tags >>
            nginx/.* image nginx overwrite_tags << pipeline.parameters.overwrite_tags >>
            nginx-es/.* image nginx-es overwrite_tags << pipeline.parameters.overwrite_tags >>
            node-exporter/.* image node-exporter overwrite_tags << pipeline.parameters.overwrite_tags >>
            openresty/.* image openresty overwrite_tags << pipeline.parameters.overwrite_tags >>
            pgbouncer/.* image pgbouncer overwrite_tags << pipeline.parameters.overwrite_tags >>
            pgbouncer-exporter/.* image pgbouncer-exporter overwrite_tags << pipeline.parameters.overwrite_tags >>
            pgbouncer-krb/.* image pgbouncer-krb overwrite_tags << pipeline.parameters.overwrite_tags >>
            po-logical-backup/.* image po-logical-backup overwrite_tags << pipeline.parameters.overwrite_tags >>
            po-pgbouncer/.* image po-pgbouncer overwrite_tags << pipeline.parameters.overwrite_tags >>
            po-spilo/.* image po-spilo overwrite_tags << pipeline.parameters.overwrite_tags >>
            postgres-exporter/.* image postgres-exporter overwrite_tags << pipeline.parameters.overwrite_tags >>
            postgres-operator/.* image postgres-operator overwrite_tags << pipeline.parameters.overwrite_tags >>
            postgresql/.* image postgresql overwrite_tags << pipeline.parameters.overwrite_tags >>
            prometheus/.* image prometheus overwrite_tags << pipeline.parameters.overwrite_tags >>
            redis/.* image redis overwrite_tags << pipeline.parameters.overwrite_tags >>
            registry/.* image registry overwrite_tags << pipeline.parameters.overwrite_tags >>
            statsd-exporter/.* image statsd-exporter overwrite_tags << pipeline.parameters.overwrite_tags >>
            vector/.* image vector overwrite_tags << pipeline.parameters.overwrite_tags >>

jobs:
  run_pre_commit:
    docker:
      - image: quay.io/astronomer/ci-pre-commit:2022-01
    steps:
      - checkout
      - run:
          name: Create pre-commit-cache-key.txt
          command: |
            cp .pre-commit-config.yaml /tmp/pre-commit-cache-key.txt
            python --version --version | sed 's/^/# /' >> /tmp/pre-commit-cache-key.txt
      - restore_cache:
          keys:
            - pre-commit-cache-{{ checksum "/tmp/pre-commit-cache-key.txt" }}
      - run:
          name: Install pre-commit hooks
          command: pre-commit install-hooks
      - save_cache:
          key: pre-commit-cache-{{ checksum "/tmp/pre-commit-cache-key.txt" }}
          paths:
            - ~/.cache/pre-commit
      - run:
          name: Run pre-commit
          command: pre-commit run --all-files --show-diff-on-failure
