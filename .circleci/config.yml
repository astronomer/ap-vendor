# Warning: automatically generated file
# Please edit .circleci/config.yml.j2, then run bin/generate_circleci_config.py
---
version: 2.1

orbs:
  python: circleci/python@2.0.3
  path-filtering: circleci/path-filtering@0.1.3

executors:
  docker-executor:
    docker:
      - image: cimg/python:3.9

parameters:
  overwrite_tags:
    type: boolean
    default: false
    description: "Set True if you want to overwrite existing docker image tag."

workflows:
  version: 2.1
  generate-config:
    jobs:
      - run_pre_commit
      - path-filtering/filter:
          base-revision: main
          config-path: .circleci/continue-config.yml
          mapping: |
            alertmanager/.* image alertmanager
            auth-sidecar/.* image auth-sidecar
            awsesproxy/.* image awsesproxy
            blackbox-exporter/.* image blackbox-exporter
            configmap-reloader/.* image configmap-reloader
            curator/.* image curator
            dind-golang/.* image dind-golang
            dogstatsd/.* image dogstatsd
            elasticsearch/.* image elasticsearch
            elasticsearch-exporter/.* image elasticsearch-exporter
            fluentd/.* image fluentd
            git-daemon/.* image git-daemon
            git-sync/.* image git-sync
            grafana/.* image grafana
            init/.* image init
            keda/.* image keda
            keda-metrics-apiserver/.* image keda-metrics-apiserver
            kibana/.* image kibana
            kube-state/.* image kube-state
            kubed/.* image kubed
            nats-exporter/.* image nats-exporter
            nats-server/.* image nats-server
            nats-streaming/.* image nats-streaming
            nginx/.* image nginx
            nginx-es/.* image nginx-es
            node-exporter/.* image node-exporter
            openresty/.* image openresty
            pgbouncer/.* image pgbouncer
            pgbouncer-exporter/.* image pgbouncer-exporter
            pgbouncer-krb/.* image pgbouncer-krb
            po-logical-backup/.* image po-logical-backup
            po-pgbouncer/.* image po-pgbouncer
            po-spilo/.* image po-spilo
            postgres-exporter/.* image postgres-exporter
            postgres-operator/.* image postgres-operator
            postgresql/.* image postgresql
            prometheus/.* image prometheus
            redis/.* image redis
            registry/.* image registry
            statsd-exporter/.* image statsd-exporter
            vector/.* image vector

jobs:
  run_pre_commit:
    docker:
      - image: quay.io/astronomer/ci-pre-commit:2022-01
    steps:
      - checkout
      - run:
          name: Create pre-commit-cache-key.txt
          command: |
            cp .pre-commit-config.yaml /tmp/pre-commit-cache-key.txt
            python --version --version | sed 's/^/# /' >> /tmp/pre-commit-cache-key.txt
      - restore_cache:
          keys:
            - pre-commit-cache-{{ checksum "/tmp/pre-commit-cache-key.txt" }}
      - run:
          name: Install pre-commit hooks
          command: pre-commit install-hooks
      - save_cache:
          key: pre-commit-cache-{{ checksum "/tmp/pre-commit-cache-key.txt" }}
          paths:
            - ~/.cache/pre-commit
      - run:
          name: Run pre-commit
          command: pre-commit run --all-files --show-diff-on-failure
