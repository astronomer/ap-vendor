---
tests:
  root_user_test: True
  default_user: git-sync
  users_config:
    - name: git-sync
      group: git-sync
      # gid,uid must match git-sync
      gid: 65533
      uid: 65533
docker:
  entrypoint: "/bin/sleep 1m"
