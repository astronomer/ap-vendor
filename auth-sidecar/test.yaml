---
tests:
  # Removed the tests as we are using distroless images and these tests run using shell
  root_user_test: False
  # default_user: nginx
  # users_config:
  #   - name: nginx
  #     group: nginx
  #     gid: 101
  #     uid: 101
